
<div class="codex-topbar-wrap">
  <header class="codex-topbar" role="banner">
    <div class="codex-topbar__inner">
      <div class="codex-topbar__brand">
        <span class="codex-topbar__eyebrow">Phoenix Vector Archive</span>
        <h1>Codex Access Node // Command Core Database</h1>
      </div>

      <a class="codex-topbar__status" href="/" aria-label="Return to Mind Arc Games">
        <span class="status-dot"></span>
        <span>www.mindarcgames.com</span>
      </a>
    </div>
  </header>
</div>

<div class="codex-topbar-spacer" aria-hidden="true"></div>

<style>
  :global(:root) {
    --codex-topbar-height: 82px;
    --codex-topbar-radius: 20px;
    --codex-topbar-z: 1000;
  }

  .codex-topbar__status {
font-family: monospace;
  text-decoration: none;
}

  .codex-topbar-wrap {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: var(--codex-topbar-z);
    pointer-events: none;
  }

  .codex-topbar {
    width: min(1400px, calc(100% - 32px));
    margin: 16px auto 0;
    min-height: var(--codex-topbar-height);
    border: 1px solid rgba(103, 232, 253, 0.2);
    border-radius: var(--codex-topbar-radius);
    background:
      linear-gradient(
        180deg,
        rgba(10, 17, 24, 0.92) 0%,
        rgba(7, 12, 18, 0.9) 100%
      );
    box-shadow:
      0 18px 50px rgba(0, 0, 0, 0.38),
      inset 0 1px 0 rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    pointer-events: auto;
  }

  .codex-topbar__inner {
    min-height: var(--codex-topbar-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.9rem 1.2rem;
  }

  .codex-topbar__brand {
    min-width: 0;
    display: grid;
    gap: 0.28rem;
  }

  .codex-topbar__eyebrow {
    color: #67e8fd;
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.72rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    line-height: 1;
  }

  .codex-topbar__brand h1 {
    margin: 0;
    color: #e6f6fb;
    font-size: clamp(0.92rem, 1.4vw, 1.05rem);
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    line-height: 1.2;
    text-wrap: balance;
  }

  .codex-topbar__status {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.55rem 0.8rem;
    border: 1px solid rgba(103, 232, 253, 0.16);
    border-radius: 999px;
    background: rgba(103, 232, 253, 0.07);
    color: rgba(230, 246, 251, 0.82);
    font-family: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    white-space: nowrap;
  }

  .status-dot {
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 50%;
    background: #67e8fd;
    box-shadow: 0 0 12px rgba(103, 232, 253, 0.75);
    flex-shrink: 0;
  }

  .codex-topbar-spacer {
    height: calc(var(--codex-topbar-height) + 32px);
  }

  @media (max-width: 920px) {
    .codex-topbar {
      width: min(100% - 24px, 1400px);
      margin-top: 12px;
    }

    .codex-topbar__inner {
      padding: 0.85rem 1rem;
    }

    .codex-topbar__status {
      display: none;
    }

    .codex-topbar-spacer {
      height: calc(var(--codex-topbar-height) + 24px);
    }
  }

  @media (max-width: 640px) {
    .codex-topbar__brand h1 {
      font-size: 0.9rem;
      letter-spacing: 0.06em;
    }

    .codex-topbar__eyebrow {
      font-size: 0.68rem;
      letter-spacing: 0.18em;
    }
  }
</style>